import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true
  // NOTE: If deploying to Firebase Hosting with SSR, you'll need additional config.
};

export default nextConfig;